# SOURCE: https://cloud.google.com/kubernetes-engine/docs/tutorials/guestbook
apiVersion: apps/v1
kind: Deployment
metadata:
  name: writer
spec:
  replicas: 1
  selector:
    matchLabels:
        app: writer
        tier: backend
  template:
    metadata:
      labels:
        app: writer
        tier: backend
    spec:
      containers:
      - name: writer
        image: kumokay/bidding:latest
        command: ["python3", "async_bidding_server.py"]
        env:
        - name: GET_HOSTS_FROM
          value: "dns"
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 50051
